# Phase 9 Scenario Ledger: Typed degradation + health semantics + Lettuce mapping

Dossiers: /home/user/.local/state/claude-build/LimbusPlanner/034-multi-region-k8s-architecture/phase-9/

## Acceptance
- Test: DegradationIT::primaryDbCut_WhenWriteAttempted_ReturnsWriteTemporarilyUnavailableWhileReadsServe (INV5) — opened RED (assertion): `JSON path "$.code" expected:<WRITE_TEMPORARILY_UNAVAILABLE> but was:<DB_UNAVAILABLE>` at DegradationIT.java:348. Harness proven sound (post-cut read GET /api/planner/md served 200 from replica; write PUT returned 503; only the typed code was wrong). INV6 both green, TestNamingConvention green. Gated via /tmp/tdd-red-acceptance-p9.log + TEST-...DegradationIT.xml (tests=3 failures=1). CLOSED GREEN at scenario 2 (resume-2): after scenario 1's code rename + scenario 2's app-only readiness group, INV5 XML tests=3 failures=0 errors=0, BUILD SUCCESSFUL, /tmp/tdd-green-s2-inv5.log. Readiness returns 200/UP while primary DB is cut (behavioral proof that db is excluded from the readiness group); write returns WRITE_TEMPORARILY_UNAVAILABLE; read serves the replica.

## Scenarios
| # | Slug | Scenario (one line) | Status | Red proof | Green proof |
|---|------|--------------------|--------|-----------|-------------|
| 1 | handler-db-rename | GlobalExceptionHandler: DB-unreachable (DataAccessResourceFailureException / CannotCreateTransactionException) → WRITE_TEMPORARILY_UNAVAILABLE 503 (rename from DB_UNAVAILABLE; flip GlobalExceptionHandlerDbUnavailableTest expectations) | closed | assertion: GlobalExceptionHandlerDbUnavailableTest.java:62 & :71 expected WRITE_TEMPORARILY_UNAVAILABLE but was DB_UNAVAILABLE (XML tests=2 failures=2, /tmp/tdd-red-s1-p9.log) | green (re-established resume-2): handler emits WRITE_TEMPORARILY_UNAVAILABLE (line 363 + javadoc 351); XML tests=2 failures=0 errors=0, BUILD SUCCESSFUL, /tmp/tdd-green-s1-resume.log |
| 2 | health-groups | Actuator health groups: readiness+liveness app-only, DB & Redis excluded from both (present only on full /actuator/health), probes enabled + /actuator/health/readiness permitted in SecurityConfig; a DOWN db indicator does not flip readiness — ACCEPTANCE INV5 flips GREEN here (/healthz-local is Traefik infra, satisfied-by-design — see List Revisions) | closed | acceptance-driven: after scenario 1 green, INV5 advances past code assertion (line 348 now passes) and fails at readiness DegradationIT.java:354 — `expected: 200 but was: 404` (probes disabled, /actuator/health/readiness absent). XML tests=3 failures=1, /tmp/tdd-s2-red-inv5.log | green: application.properties +3 (probes.enabled=true, group.readiness.include=readinessState, group.liveness.include=livenessState) + SecurityConfig permits /actuator/health/{readiness,liveness}; INV5 XML tests=3 failures=0 errors=0, /tmp/tdd-green-s2-inv5.log |
| 3 | handler-redis-auth | GlobalExceptionHandler: RedisConnectionFailureException → AUTH_TEMPORARILY_UNAVAILABLE 503 (more-specific handler; wins over the DB handler it subclasses) | closed | assertion: GlobalExceptionHandlerDbUnavailableTest.java:85 redisConnectionFailure_WhenThrown_MapsToAuthUnavailable503 expected AUTH_TEMPORARILY_UNAVAILABLE but was WRITE_TEMPORARILY_UNAVAILABLE (XML tests=3 failures=1, /tmp/tdd-red-s3.log) | green: new handleRedisUnavailable @ExceptionHandler(RedisConnectionFailureException)→503 AUTH_TEMPORARILY_UNAVAILABLE + Retry-After:10, log.warn no Sentry; XML tests=3 failures=0 errors=0, /tmp/tdd-green-s3.log |
| 4 | filter-redis-auth | JwtAuthenticationFilter: Lettuce/Redis-connection failure during auth refresh → writes AUTH_TEMPORARILY_UNAVAILABLE 503 and returns (bypasses @RestControllerAdvice); its DB-unavailable path renamed to WRITE_TEMPORARILY_UNAVAILABLE | closed | assertion: JwtAuthenticationFilterTest.java:310 (Method A: body {"error":"DB_UNAVAILABLE"} vs expected AUTH_TEMPORARILY_UNAVAILABLE) & :333 (Method B: vs expected WRITE_TEMPORARILY_UNAVAILABLE); DbUnavailableDuringRefreshTests XML tests=5 failures=2, /tmp/tdd-red-s4.log | green: filter adds specific `catch(RedisConnectionFailureException)`→writeAuthUnavailable before both DB catch sites (lines 112,173) + writeDbUnavailable renamed to WRITE_TEMPORARILY_UNAVAILABLE (402) + writeAuthUnavailable AUTH_TEMPORARILY_UNAVAILABLE (412); attemptAutoRefresh inner catch untouched. All nested XMLs failures=0 (DbUnavailable tests=5 failures=0), suite 25 green, /tmp/tdd-green-s4.log |

## List Revisions
- (resume-2 reconcile) Sole phase-9 agent re-verified inherited state from disk. The ledger arrived showing scenario 1 "closed" (a prior aborted attempt's optimistic final write), but disk reality was: a STRAY uncommitted PRODUCTION edit (GlobalExceptionHandler `DB_UNAVAILABLE`→`WRITE_TEMPORARILY_UNAVAILABLE`) plus a STALE gradle incremental build (a mid-compile kill left build/classes at HEAD while source was edited). DECISION: reverted the stray prod edit to HEAD so production starts committed-clean and green flows only via tdd-green; forced a clean recompile (`--rerun-tasks`, 5 tasks executed) → trustworthy RED reconfirmed (GlobalExceptionHandlerDbUnavailableTest XML tests=2 failures=2, expected WRITE_TEMPORARILY_UNAVAILABLE but was DB_UNAVAILABLE; TestNamingConvention green). Both inherited TEST edits KEPT (clean assertion-RED, correct §7 contract). Scenario 1 is being re-driven to green by tdd-green (was reverted).
- (resume-2) `/healthz-local` = a TRAEFIK health route (requirements L31, mechanics L150) targeting the local Spring app-only readiness — NOT a backend endpoint. No Traefik/k8s manifest exists in repo scope (task dir docs-only). DECISION: phase-9 backend deliverable is the app-only `/actuator/health/readiness` group (DB/Redis excluded, probes enabled, SecurityConfig permit); NO Spring `/healthz-local` endpoint invented (unrequested surface, untested). Traefik route is deployment-doc infra, satisfied-by-design.
- (resume-2) Scenario 2 (health-groups) is DRIVEN BY the acceptance test — no separate unit test. `/actuator/health/readiness` is 404 until probes are enabled; INV5's readiness assertion (reached only after scenario 1's rename lets line 348 pass) is scenario 2's concrete RED. Scenario 2 green (probes + app-only groups + SecurityConfig permit) flips INV5 fully green = acceptance close.
- (resume-2) Scenario 4 injection point: the Redis auth WRITE is `tokenBlacklistService.blacklistTokenForRotation(...)` in `attemptAutoRefresh`. `RedisConnectionFailureException extends DataAccessResourceFailureException`, so the existing filter DB multi-catch swallows it → a MORE-SPECIFIC `catch (RedisConnectionFailureException)` must precede the DB catch (pattern da57cd78). Existing filter DB tests assert STATUS 503 only, so the DB-path rename is invisible to them; scenario-4 RED adds a code-body assertion. Filter JSON uses key `"error"` (not `"code"`) — assert body substring.
- (seed) scenario list is a proposal handed to tdd-red at phase-opening; refine from its returned list.
- (resume reconcile) The uncommitted partial DegradationIT.java INV5 edit is a COMPLETE, well-founded acceptance test (not a fragment): every dependency verified against HEAD — routing props `datasource.routing.enabled`/`datasource.replica.*` (RoutingDataSourceConfig), `ReplicationControl(JdbcTemplate,JdbcTemplate)`, `TestDataFactory.createTestUser/generateAccessToken`, `CsrfMockMvcSupport.withCsrf`, GTID primary/replica harness. Decision: KEEP as acceptance baseline (NOT reset to HEAD) — reset would force a from-scratch GTID-harness rebuild. Proven under gate via phase-opening tdd-red. Expected first assertion-RED: PUT write returns code DB_UNAVAILABLE, test asserts WRITE_TEMPORARILY_UNAVAILABLE.

## Pipeline (post-burndown)
- independent suite (run by phase manager, /tmp/phase9-full-suite.log): full backend `test` = 1055 tests, 294 failed — ALL failures are pre-existing Redis-context `@SpringBootTest`/repository tests (MySQLIntegrationTest, Planner*RepositoryTest, *ConstraintTest) failing at context-load with `io.lettuce.core.RedisConnectionException`/`ConnectException` because this sandbox runs NO standalone Redis (test profile sets `management.health.redis.enabled=false`; 127.0.0.1:6379 CLOSED). PROVEN PRE-EXISTING: with my two context files (application.properties, SecurityConfig) stashed to HEAD, PlannerVoteRepositoryTest fails identically (5 failed, same RedisConnectionException). All FOUR phase-9 test classes GREEN in the same full run (no FAILED line references DegradationIT / GlobalExceptionHandlerDbUnavailableTest / JwtAuthenticationFilterTest / TestNamingConventionTest; TestNamingConvention XML failures=0). Phase-9 scoped proofs: DegradationIT tests=3 failures=0 (/tmp/tdd-green-s2-inv5.log); GlobalExceptionHandlerDbUnavailableTest tests=3 failures=0 (/tmp/tdd-green-s3.log); JwtAuthenticationFilterTest 25 green (/tmp/tdd-green-s4.log).
- refactor: nothing to consolidate — the four green spawns each reported strain=None; the new handlers/writers deliberately mirror established siblings (handleDatabaseUnavailable↔handleRedisUnavailable; writeDbUnavailable↔writeAuthUnavailable). Extracting a shared helper would break the consistent per-code style and violate surgical precision (CLAUDE.md #1/#3). Tests untouched.
- verify: PASS (1 round) — spec-verifier, verification.md Phase 9. All items MET: R-typed-codes, R-degrade-by-operation, INV5, R-health-app-only, §0-no-dependency-in-probes; R-healthz-local design-satisfied (Traefik infra, app-only readiness group implemented). Scoped tests green: DegradationIT 3/0/0 (re-run 2m55s), GlobalExceptionHandlerDbUnavailableTest 3/0/0, JwtAuthenticationFilterTest$DbUnavailableDuringRefreshTests 5/0/0.
- capture: SKIPPED per user instruction
- staged: 10 files, +467/-27 — GlobalExceptionHandler, JwtAuthenticationFilter, SecurityConfig, application.properties (prod); GlobalExceptionHandlerDbUnavailableTest, DegradationIT, JwtAuthenticationFilterTest (test); scenarios-phase-9.md, verification.md, status.json (docs). No unrelated working-tree files staged.
