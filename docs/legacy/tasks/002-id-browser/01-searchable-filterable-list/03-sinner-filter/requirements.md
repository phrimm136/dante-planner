# Task: Sinner Filter

## Description
Edit the sinner filter placeholder to the real one so that users can filter the identities by sinners. Show all identities when no sinners are selected. Show identities of all selected sinners. Each buttons is consisted of the corresponding sinner icon in `/static/images/sinners/`. Note that the filtering have to be conducted by watching {id}.sinner value in `identitySpecList.json`. Rename the filter component name to `IdentitySinnerFilter`.

## Research

## Scope
- `/static/data/identitySpecList.json`
- `/frontend/src/components/identity/IdentityCharacterFilter.tsx`

## Target Code Area
- `/frontend/src/components/identity/IdentityCharacterFilter.tsx`

## Testing Guidelines
