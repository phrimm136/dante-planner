# Phase 03: Rotation + facade + logout-all routing to typed parsers

- Kind: local-tdd — rotation/facade/controller unit tests (`@ExtendWith(MockitoExtension.class)`), no containers. Run:
  `./gradlew -p backend test -PexcludeTags=containerized --tests "org.danteplanner.backend.auth.token.RefreshRotationServiceTest" --tests "org.danteplanner.backend.facade.*" --tests "org.danteplanner.backend.controller.AuthControllerLogoutAllTest"`
- Files:
  - `backend/src/main/java/org/danteplanner/backend/auth/token/RefreshRotationService.java` — route `:188` (presented refresh), `:219` (self-minted successor), `:241` (stored refresh from Redis) to `tokenValidator.validateRefreshToken`. Keep the existing `!claims.isRefreshToken()` guard at `:196` (now belt-and-suspenders behind the refresh parser — do NOT remove it).
  - `backend/src/main/java/org/danteplanner/backend/auth/facade/AuthenticationFacade.java` — route `:159` (refreshTokens) and `:259` (logout, refresh) to `validateRefreshToken`; `:248` (logout, access) and `:291` (logout-all, access) to `validateAccessToken`. (Now an explicit "Create or modify" file in the amended Scope.)
- Tests (existing suites `@Mock` `TokenValidator` and stub `.validateToken(...)`; the re-route makes those stubs stale — migrating them to the typed methods is in-scope implementation):
  - `backend/src/test/java/org/danteplanner/backend/auth/token/RefreshRotationServiceTest.java` — stale-stub migration + new rotation negative/round-trip cases (TW3, TW5b, B10/B11).
  - `backend/src/test/java/org/danteplanner/backend/facade/AuthenticationFacadeTest.java` — stale-stub migration of the mocked positive cases, PLUS the new real-token wrong-type negative cases (TW6, below) that must NOT use the `TokenValidator` mock.
  - `backend/src/test/java/org/danteplanner/backend/facade/AuthenticationFacadeLineageTest.java` — stale-stub migration only.
  - `backend/src/test/java/org/danteplanner/backend/controller/AuthControllerLogoutAllTest.java` — stale-stub migration only (`:291` now `validateAccessToken`).
- External contract: an access JWT presented to `RefreshRotationService.rotate` is rejected as `RotationResult.Rejected(INVALID)` — no successor minted, no Redis family key mutated, cookies untouched. A self-minted successor (`:219`) and a stored refresh JWT replayed on retry (`:241`) still validate on the refresh parser (rotation does not break). At the `AuthenticationFacade` seam, each call site rejects the wrong token type: the refresh-handling sites (`refreshTokens :159`, `logout` refresh branch `:259`) reject an access JWT, and the access-handling sites (`logout` access branch `:248`, `logoutAll :291`) reject a refresh JWT. Correctly-typed tokens behave exactly as before.
- Behavior inventory (Seam 3 — `RefreshRotationService` internal validations, migration-critical):
  - B10 `rotate :219, :241` — "Successor & stored refresh JWTs (self-minted, `type=refresh`) validate successfully": preserved — MUST NOT break (INV4). Characterization / migration net: `RefreshRotationServiceTest` — a full rotation still produces a valid successor and a retry replays the stored JWT.
  - B11 `rotate :188-196` — "Presented refresh token validated, then `isRefreshToken()` guard rejects access tokens before the legacy branch": preserved; the guard becomes belt-and-suspenders behind the refresh parser. Characterization: `RefreshRotationServiceTest` — an access token is rejected (now at the parser) and never reaches the legacy-admit branch.
- Test allocation notes:
  - TW3 (INV2 rotation half): `RefreshRotationServiceTest` — real `rotate` with access-token claims → `Rejected(INVALID)`, verify NO `generateRefreshToken` call and NO Redis script execution (family untouched). This is the authoritative INV2 rotation coverage; the mocked filter-lineage case in phase 02 cannot prove "no family touched."
  - TW5b (INV4 successor/stored half): `RefreshRotationServiceTest` — successor (`:219`) and stored (`:241`) refresh JWTs validate on `validateRefreshToken`.
  - TW6 (facade real-token wrong-type rejection — amended Test Plan item): `AuthenticationFacadeTest` — four negative cases proven with REAL tokens (a real `JwtTokenService`/typed validator, NOT the `TokenValidator` mock, so the typed parser actually throws `INVALID_TYPE`): an access JWT into `refreshTokens` (`:159`) and into `logout`'s refresh slot (`:259`) is rejected; a refresh JWT into `logout`'s access slot (`:248`) and into `logoutAll` (`:291`) is rejected. This closes the stale-stub blind spot — a mock validator cannot enforce the type, so these cases must bypass the mock. Assert the wrong-type token is not blacklisted / does not drive its side effect.
  - Facade positive-path correctness (correctly-typed refresh/logout still works) remains guarded by the migrated existing `AuthenticationFacadeTest` / `AuthenticationFacadeLineageTest` / `AuthControllerLogoutAllTest` cases plus the INV4 round-trip; TW6 adds the missing NEGATIVE coverage the mock could never provide.
- Mechanics sections:
  - `mechanics.md §2` (call-site routing, binding) — rows `RefreshRotationService.java:188/:219/:241` → `refreshParser`; `AuthenticationFacade.java:159/:259` → `refreshParser`, `:248/:291` → `accessParser`. A refresh row mis-routed to `accessParser` is a mass logout (breaks the 7-day session).
  - `mechanics.md §2` lineage-path note — keep the `!claims.isRefreshToken()` guard at `:196`.
  - `mechanics.md §4` (migration-safety proof) — the `admitLegacy`/`LEGACY_*` machinery is a DIFFERENT backward-compat axis (null `jti`/`familyId`), unrelated to the `type` claim; needs no change here.
- Considerations:
  - requirements INV2 — an access JWT MUST NOT drive rotation: no successor minted, no family touched (the phase headline, TW3).
  - requirements INV4 — the rotation service's own successor/stored refresh JWTs continue to validate; this is the migration net that proves the re-route does not self-break refresh.
  - requirements Test Plan (amended) — the facade negative cases must use real tokens, not `TokenValidator` stubs; a mocked validator returns a canned `TokenClaims` and would never exercise the parser's type enforcement, so the mock path is exactly the blind spot this item exists to close.
  - requirements Decision 1 / Behavior Inventory B11 — the parser is the primary enforcement; the `isRefreshToken` guard stays as belt-and-suspenders, preserving the recorded `rotate()`-legacy-branch fix.
  - `backend/src/test/CLAUDE.md` — unit tier, `MockitoExtension`, fixed `Clock`; `@Transactional` rollback blocks AFTER_COMMIT listeners (not relevant here, but keep tests container-free). For TW6, wire a real `JwtTokenService` with a test key + fixed `Clock` so generated tokens carry the real `type` claim.
- Depends on: 01 (needs `validateAccessToken` / `validateRefreshToken` on the interface).
- Verify: `./gradlew -p backend test -PexcludeTags=containerized --tests "org.danteplanner.backend.auth.token.RefreshRotationServiceTest" --tests "org.danteplanner.backend.facade.*" --tests "org.danteplanner.backend.controller.AuthControllerLogoutAllTest"` green — the facade/controller suites are OUTSIDE the Test Plan's scoped command and must be named explicitly or a stale-stub break (or a missing TW6 negative case) ships silently.
