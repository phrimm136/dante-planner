# Epic: Project Initialization

## Description
Initialize the monolithic repository and ensure both frontend (React 19, Vite, Yarn, Tailwind, shadcn/ui, TanStack Query + Router, Vitest) and backend (Spring Boot, MySQL, JPA, Redis, OAuth 2.0) environments are correctly configured and can run locally and communicate via API.

## Dependencies
None — this is the first epic.