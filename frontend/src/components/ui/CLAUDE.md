# ui/ primitives

shadcn/ui primitives plus a few project text/icon components. Do not assume a primitive exists — check this directory first. Notably absent: `card`, `alert-dialog`, `select` — do not import them.

Add missing primitives with the shadcn CLI (`components.json` is configured); never hand-roll a primitive shadcn provides.
