import type { CardGeometry } from '@/shared/cardLayout'

/** The home page's recently-released tile, which is square. */
export const RECENT_RELEASE_GEOMETRY: CardGeometry = {
  size: { widthPx: 112, heightPx: 112 },
  mobileScale: 1,
  rows: 'content',
}
