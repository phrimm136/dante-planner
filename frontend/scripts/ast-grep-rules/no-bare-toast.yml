id: no-bare-toast
language: TypeScript
severity: error
message: A bare toast.error/toast.success bypasses error classification and the opt-in support hint; call showError, showAppError, showErrorMessage or showSuccess from lib/errorPresentation.
files:
  - src/**/*.ts
ignores:
  - src/lib/errorPresentation.ts
  - src/lib/toast.ts
rule:
  any:
    - pattern: toast.error($$$ARGS)
    - pattern: toast.success($$$ARGS)
