id: no-throw-in-lib
language: TypeScript
severity: error
message: lib is the functional core; return a Result, throw only in boundary adapters.
files:
  - src/pages/*/lib/**
  - src/shared/*/lib/**
ignores:
  - '**/__tests__/**'
rule:
  pattern: throw $ERR
