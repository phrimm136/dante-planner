# Never commit real values or state to this public repo.
*.tfstate
*.tfstate.*
.terraform/
# .terraform.lock.hcl IS committed (pins provider version + hashes for reproducible init).
terraform.tfvars
*.auto.tfvars
# the example placeholder file IS committed:
!terraform.tfvars.example
crash.log
